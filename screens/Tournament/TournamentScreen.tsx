import React from "react";
import { Text, View } from "react-native";

type TTournamentScreenProps = {};

const TournamentScreen: React.FC<TTournamentScreenProps> = ({}) => {
  return (
    <View>
      <Text>TournamentScreen</Text>
    </View>
  );
};

export default TournamentScreen;
