import React from "react";
import { StackNavigation } from "../Navigation/StackNavigation";

type TTournamentScreenProps = {};
export const TournamentScreen: React.FC<TTournamentScreenProps> = ({}) => {
  return <StackNavigation />;
};
